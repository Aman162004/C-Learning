#include <stdio.h>
#include <string.h>
void count(char* str, int* character, int* digit, int* vowels, int* space, int* size){
    for (int i = 0; i < size; i++)
    {
    if ((int)str[i] >64 && (int)str[i]<91)
        if ((int)str[i] >96 && (int)str[i]<123)
        if ((int)str[i] >96 && (int)str[i]<123)
        {
            
        }
        
    }
    
}
int main(int argc, char const *argv[])
{
    
    return 0;
}
